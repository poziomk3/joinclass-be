spring:
  application:
    name: JoinClass
  jpa:
    hibernate:
      ddl-auto: update
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://accounts.google.com

springdoc:
  swagger-ui:
    url: /openapi.yaml
    path: /swagger-ui.html